{
  "name": "graphql-gateway",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/graphql/gateway/src",
  "projectType": "application",
  "tags": ["type:app", "kind:graphql"],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/graphql/gateway",
        "format": ["cjs"],
        "bundle": false,
        "main": "apps/graphql/gateway/src/main.ts",
        "tsConfig": "apps/graphql/gateway/tsconfig.app.json",
        "assets": [
          {
            "glob": "**/*.graphql",
            "input": "{projectRoot}/src",
            "output": "apps/graphql/gateway/src/"
          }
        ],
        "generatePackageJson": true,
        "esbuildOptions": {
          "sourcemap": true,
          "outExtension": {
            ".js": ".js"
          }
        }
      },
      "configurations": {
        "development": {},
        "production": {
          "generateLockfile": true,
          "esbuildOptions": {
            "sourcemap": false,
            "outExtension": {
              ".js": ".js"
            }
          }
        }
      }
    },
    "compose": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": [
        "!{workspaceRoot}/apps/graphql/gateway/**/*",
        "{workspaceRoot}/apps/graphql/**/schema.graphql"
      ],
      "outputs": ["{projectRoot}/src/supergraph.graphql"],
      "options": {
        "command": "rover supergraph compose --config {projectRoot}/src/config/supergraph.local.yaml --output {projectRoot}/src/supergraph.graphql --elv2-license=accept"
      }
    },
    "docker": {
      "executor": "@plugins/workspace:docker",
      "outputs": ["{option.outputPath}"],
      "dependsOn": ["build"],
      "options": {
        "outputPath": "dist/apps/graphql/gateway"
      }
    },
    "k8s": {
      "executor": "@plugins/workspace:k8s",
      "outputs": ["{option.outputPath}"],
      "options": {
        "outputPath": ".nexa/graphql-gateway/k8s"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "run": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "graphql-gateway:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "graphql-gateway:build:development"
        },
        "production": {
          "buildTarget": "graphql-gateway:build:production"
        }
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": true,
        "commands": [
          {
            "command": "nx run graphql-gateway:run",
            "color": "magenta"
          },
          {
            "command": "nx run graphql-users:serve",
            "color": "blue"
          },
          {
            "command": "nx run graphql-projects:serve",
            "color": "green"
          }
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/graphql/gateway/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "apollo-install": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "node scripts/install-apollo-router.js",
          "node scripts/install-apollo-rover.js"
        ]
      }
    },
    "apollo-router": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "development",
      "options": {
        "parallel": true
      },
      "configurations": {
        "subgraphs": {
          "commands": [
            {
              "command": "nx serve graphql-users",
              "color": "blue"
            },
            {
              "command": "nx serve graphql-projects",
              "color": "green"
            }
          ]
        },
        "development": {
          "commands": [
            "nx run graphql-gateway:subgraphs",
            "tmp/router --dev --supergraph {projectRoot}/src/supergraph.graphql"
          ]
        },
        "production": {
          "commands": ["tmp/router --supergraph {projectRoot}/src/supergraph-production.graphql"]
        }
      }
    }
  }
}
