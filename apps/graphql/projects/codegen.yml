schema:
  - http://localhost:4001
documents: []
overwrite: true
generates:
  ./src/graphql/interfaces/types.ts:
    config:
      scalars:
        Date: Date
        DateTime: Date
    plugins:
      - 'typescript'
require: []
