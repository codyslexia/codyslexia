# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build auth-webauthn`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t auth-webauthn`.
FROM docker.io/node:22-alpine3.19

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system auth-webauthn && \
  adduser --system -G auth-webauthn auth-webauthn

COPY dist/apps/auth/webauthn auth-webauthn
RUN chown -R auth-webauthn:auth-webauthn .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix auth-webauthn --omit=dev -f install

CMD [ "node", "auth-webauthn" ]
